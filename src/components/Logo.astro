---
const {className, id, isFaded = true} = Astro.props
---


<img src="logo/logo.webp" class={isFaded ? `${className} faded` : className} id={id} />

<style>

    img
    {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        height: 250px;
        width: 250px;
        z-index: 3000;
    }

    .faded
    {
        opacity: .3;
    }
</style>