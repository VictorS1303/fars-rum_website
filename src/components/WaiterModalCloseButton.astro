<i class="fas fa-times modal-close-button fa-2x" id="modal_close_button"></i>

<style>
    .modal-close-button
    {
        font-size: 20px;
        position: absolute;
        top: 30px;
        right: 30px;
        color: var(--border-color);
        cursor: pointer;
        z-index: 10000;
    }
</style>

<script>
    const waiterModalCloseButton = document.querySelector('.modal-close-button')
    const waiterModal = document.querySelector('#waiter_modal_container')

    waiterModalCloseButton.addEventListener('click', (e) => closeWaiterModal(e))

    function closeWaiterModal(e: any)
    {
        console.log(123)
        e.target.closest('.waiter-modal-container').classList.remove('opened')
    }
</script>